import { createTheme } from '@mui/material/styles';

export const defaultTheme = createTheme({
	palette: {
		primary: {
			main: '#C00734',
		},
		secondary: {
			main: '#FFB8B1',
		},
		cta: {
			main: '#FA0000',
		},
	},
});
